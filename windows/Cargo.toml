[package]
name = "airpod-pc-audio"
version = "0.1.0"
edition = "2021"

[dependencies]
cpal = "0.15"
anyhow = "1.0"
crossbeam-channel = "0.5"
ctrlc = "3.4"

[target.'cfg(windows)'.dependencies]
# Windows-specific if needed later
